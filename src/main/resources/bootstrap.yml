spring:
  cloud:
    config:
      name:  test-config,errorcode-constants,db,eureka
      uri: http://localhost:8080
  profiles:
    active: local
  application:
    name:  test-config
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://localhost:9411/